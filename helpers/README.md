# Helpers Module

*This file was generated by **gpt-oss-120b***

## Purpose

The **`helpers`** directory contains a collection of utility functions for the XBeach processing pipeline.  

## Files

| File | Description |
|------|-------------|
| `helpers.py` | Defines the `HelperFuncs` class, which provides common functionality used across the pipeline, such as path handling, data loading, and preprocessing utilities. |
| `__pycache__/` | Python byte‑code cache generated at runtime (no source code). |

## Usage Example
```python
from helpers.helpers import HelperFuncs

# Instantiate the helper utilities
utils = HelperFuncs()
# Example usage of a helper method
utils.some_common_method()
```

The class can be instantiated wherever needed (e.g., in `main.py`) and its methods called to facilitate various processing steps.

### All Functions

| Function | Description |
|----------|-------------|
| `read_paths` | Reads key‑value paths from `paths.txt`. |
| `check_hotstart` | Checks if the model run is a hotstart run by looking for params.txt. |
| `get_first_model_dir` | Returns the path to the first model directory (useful for hotstart runs). |
| `get_output_filename` | Retrieves the primary NetCDF output filename. |
| `get_dataset` | Loads the model output as an xarray Dataset, handling hot‑starts. |
| `get_figsize` | Determines figure size based on the domain being considered. |
| `get_hotstart_ds` | Opens and concatenates hot‑start NetCDF files, fixing time indices. |
| `read_max_xarray` | Reads the maximum value from the xarray dataset. |
| `read_dims_xarray` | Reads the dimensions of the XBeach output. |
| `get_resolution` | Reads dx and dy from params.txt. |
| `get_origin` | Reads xo, yo, and theta from params.txt. |
| `read_from_params` | Reads a specific variable from `params.txt`. |
| `read_transect_data_xarray` | Loads a transect from the xarray output dataset. |
| `read_3d_data_xarray` | Reads the full 3D array (time, y, x) from the NetCDF output file. |
| `read_3d_data_xarray_nonmem` | Reads the full 3D DataArray without loading all into memory. |
| `read_2d_data_xarray_timestep` | Reads xarray data for entire domain at specified time step. |
| `read_pt_data_xarray` | Reads xarray data at a specific point. |
| `read_npy` | Reads `.npy` files from the results directory. |
| `read_dat` | Reads `.dat` files from the results directory. |
| `read_time_xarray` | Reads time from the XBeach output. |
| `reproject_raster` | Reprojects raster from current EPSG to local UTM EPSG. |
| `read_removed_bldgs` | Reads removed buildings mask from various possible files. |
| `read_buildings` | Reads building grid (z.grd) and returns a masked array where buildings are present. |
| `frcing_to_dataframe` | Reads XBeach forcing file and returns a pandas DataFrame. |
| `read_grid` | Reads the x, y, and z grids. |
| `read_coast` | Reads coastline geodataframe. |
| `read_bldgs_geodataframe` | Reads building geodataframe and sets VDA_id as index. |
| `xy_to_grid_index` | Returns the indices in xgr, ygr that are nearest to the xy points. |
| `tstartstop_to_tindex` | Returns the indices nearest to the provided start and stop times (in hours). |
| `time_to_tindex` | Returns the index nearest to the provided time (in seconds). |
| `save_fig` | Saves figures to the results directory. |
| `get_H` | Returns an array of wave heights from a time series of water elevations. |
| `get_T` | Returns an array of periods from a time series of water elevations. |
| `assign_max_to_bldgs` | Assigns maximum value to buildings. |
| `compute_Hs` | Computes significant wave height (mean of highest 1/3) from array of wave heights. |
| `cartesian_to_nautical_angle` | Converting from cartesian to nautical angles. |
| `nautical_to_xbeach_angle` | Converting from nautical to XBeach angle using shoreline orientation alfa. |
| `make_directory` | Make directory if it doesn't exist. |
| `var2label` | Returns label, units, and color for a given variable. |
| `xbeach_duration_to_start_stop` | Maps simulation duration to recommended start/stop analysis times. |
| `remove_frame` | Removes spines and ticks from an axis. |
| `check_domain_size_wave_stat` | Checks that the domain size of two runs is identical. Resizes if necessary. |
| `set_hotstart_runs` | Finds all hotstart directories in the model path. |
| `rmse` | Calculates Root Mean Square Error. |
| `mae` | Calculates Mean Absolute Error. |
| `get_elevated_bldgs` | Separates elevated from non‑elevated buildings. |
| `compute_velocity_mag` | Computes velocity magnitude from x and y components. |
| `compute_velocity_dir` | Computes velocity direction at the time of maximum magnitude. |
| `compute_angle` | Calculates the angle [0, 360) of a 2D vector. |
| `calculate_running_avg` | Calculates a running average and optionally resamples. |
