# Process Uplift Forces Elevated Module

*This file was generated by **gpt-oss-120b***

## Purpose

The **`process_uplift_forces_elevated`** directory contains a self‑contained utility for processing uplift forces on elevated structures within the XBeach model. It reads damage data, merges it with building statistics, computes freeboard and surge wave interactions, and determines which elevated buildings should be flagged for removal based on predefined criteria.

## Files

| File | Description |
|------|-------------|
| `process_uplift_forces_elevated.py` | Defines the `ProcessUpliftForcesElevated` class (sub‑class of `HelperFuncs`). The class loads damage CSVs, merges them with building data, calculates freeboard‑surge wave differences, applies removal logic for pre‑1974 elevated buildings, and writes a `removed_elevated_bldgs.csv` file containing the results. |
| `__init__.py` | Makes the directory a Python package; currently empty. |
| `__pycache__/` | Python byte‑code cache generated at runtime (no source code). |

## Usage Example
```python
from process_uplift_forces_elevated.process_uplift_forces_elevated import ProcessUpliftForcesElevated

# The class automatically picks up the paths defined in HelperFuncs
uplift = ProcessUpliftForcesElevated()
uplift.process()
```

The class can be instantiated wherever needed (e.g., in a main workflow script). Calling the `process` method will generate the `removed_elevated_bldgs.csv` file in the configured results directory.
